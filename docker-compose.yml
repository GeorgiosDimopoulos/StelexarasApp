services:
  api:
    build:
      context: ./StelexarasApp.API
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    restart: always
    networks:
      - app-network
    depends_on:
      - db
  db:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      SA_PASSWORD: "SA_PASSWORD"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    networks:
      - app-network

  frontend:
    build:
      context: ./StelexarasApp.Web
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: always
    networks:
      - app-network
    depends_on:
      - api
      
  portainer:
    image: portainer/portainer-ce
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    restart: always
networks:
  app-network:
    driver: bridge
volumes:
  portainer_data: